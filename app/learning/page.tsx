import type { Metadata } from "next";
import { SiteShell } from "../components/SiteShell";

export const metadata: Metadata = { title: "Learning Structure", description: "Explore the ChantCode learning structure for 36 core multiplication facts across the 2s through 9s.", alternates: { canonical: "/learning" }, openGraph: { title: "ChantCode Learning Structure", description: "36 core facts organized for listening, repetition, recall, testing, and practice.", url: "/learning" } };
const stages = ["Listen", "Repeat", "Prompted Recall", "Independent Recall", "Practice"];
export default function LearningPage() { return <SiteShell><main className="content-main"><header className="content-hero"><p className="eyebrow">Learning Structure</p><h1>36 core multiplication facts</h1><p className="content-lede">ChantCode organizes the non-duplicate 2s–9s multiplication facts into a compact sequence for memorization and recall.</p></header><section className="learning-count"><strong>36</strong><div><h2>Why 36?</h2><p>The 1s facts follow the rule “1 × a number = that number.” Multiplication is also commutative, so one stored fact can support both factor orders. After the 1s and reversed duplicates are removed from the traditional 9×9 set, 36 core facts remain for concentrated memorization.</p><p>For example, one stored chant line for 6 × 7 = 42 can support practice with both 6 × 7 and 7 × 6.</p></div></section><section className="learning-path"><p className="eyebrow">The Learning Path</p><div>{stages.map((stage, index) => <article key={stage}><span>{String(index + 1).padStart(2, "0")}</span><h2>{stage}</h2></article>)}</div></section><p className="learning-note">Ordered recall is a starting point—not the final goal. Random and mixed retrieval are needed before a child can access facts independently. The website explains this structure; practice takes place in the ChantCode App.</p></main></SiteShell>; }
