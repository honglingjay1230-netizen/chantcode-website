// Public endpoint only. Sandbox builds replace this file during packaging.
export const REFERRAL_ORIGIN = 'https://chantcode.com';
// Keep release entry points closed until the real Apple Sandbox flow passes.
export const REFERRALS_ENABLED = false;
